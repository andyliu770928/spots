# Spots 專案架構記錄

## 2026-05-05 Supabase → Neon 遷移

### 背景
Supabase 美國區域即將關閉，必須遷移到 Neon Postgres。

---

## 目前資料庫架構

### 供應商：Neon（serverless Postgres）
- Connection string 儲存在 `DATABASE_URL` 環境變數
- 不需要 Auth，只用 API key（`SPOTS_INGEST_SECRET`）

### 資料表
- `places` — 景點/美食卡片
- `trips` — 行程
- `trip_items` — 行程中的地點關聯

---

## 程式碼架構（已改為 Neon）

### 不再使用的檔案（已刪除）
- `src/lib/supabase/client.ts` — 刪除
- `src/lib/supabase/server.ts` — 刪除
- `src/lib/api.ts` — 刪除

### 現有核心檔案

| 檔案 | 功能 |
|------|------|
| `src/lib/db.ts` | 資料庫連線（唯一），使用 `postgres` 套件 |
| `src/lib/places-server.ts` | 處理 URL 解析、欄位補完、API 授權 |
| `src/lib/places-api.ts` | client-side API helper（fetch wrapper，避免 server-only postgres 進入瀏覽器）|
| `src/lib/places.ts` | 純資料處理（解析 tags、address、category 等） |
| `src/app/api/places-list/route.ts` | GET all places（給前端頁面用）|
| `src/app/api/places-list/add/route.ts` | POST 新增 place（給前端頁面用）|
| `src/app/api/places/capture/route.ts` | POST capture API（给 spots_enrich.py 用）|
| `src/app/api/places/ingest/route.ts` | POST ingest API（備用）|
| `src/app/api/places/[id]/route.ts` | GET/PATCH single place |

### 前端頁面
- `src/app/page.tsx` — 首頁（使用 `placesApi` 而非 `api`）
- `src/app/places/[id]/page.tsx` — 詳情頁（使用 `placesApi`）

---

## DB 查詢寫法

使用 `postgres` 套件，`client.unsafe()` 執行原始 SQL：

```typescript
// fetch all
const rows = await client`SELECT * FROM places ORDER BY created_at DESC`

// insert
const [data] = await client.unsafe(
  `INSERT INTO places (col1, col2) VALUES ($1, $2) RETURNING *`,
  [value1, value2]
)

// update
const [data] = await client.unsafe(
  `UPDATE places SET col = $1 WHERE id = $2 RETURNING *`,
  [value, id]
)
```

### 不再使用的寫法（Supabase 鏈式語法）
```typescript
// 舊：Supabase chain
supabase.from('places').select('*').eq('id', id)

// 新：postgres + unsafe
client`SELECT * FROM places WHERE id = ${id}`
```

---

## 遷移時做的改動

### 套件更換
- 移除：`@supabase/supabase-js`、`@supabase/ssr`
- 安裝：`postgres`（用於 server-side）、`pg`（用於資料匯入 script）

### API routes 修改
- `capture/route.ts` — insert 改用 `client.unsafe()`
- `ingest/route.ts` — insert 改用 `client.unsafe()`
- `[id]/route.ts` — GET/PATCH 改用 `client.unsafe()` 或 tagged template

### 避免 browser bundle 問題
- `postgres` 是 server-only 的，會用到 node.js 的 `fs`、`net` 等模組
- 前端 client 不能直接 import，所以：
  - `src/lib/api.ts`（使用 `createClient` from Supabase）已刪除
  - 新增 `src/lib/places-api.ts` 作為 fetch wrapper，前端用它打 API
  - 前端頁面（`page.tsx`、`places/[id]/page.tsx`）改用 `placesApi`

---

## 環境變數

| 變數 | 用途 |
|------|------|
| `DATABASE_URL` | Neon Postgres connection string |
| `SPOTS_INGEST_SECRET` | API 授权密钥 |

---

## 未來如果需要修改

### 如果要改 DB query
→ 編輯 `src/app/api/places/` 下的各個 route.ts
→ 使用 `client.unsafe(query, values)` 或 tagged template

### 如果要改前端 API call 方式
→ 編輯 `src/lib/places-api.ts`

### 如果要新增資料庫功能
→ 先確認 Neon 有足夠的免費額度（0.5GB）
→ SQL 直接在 Neon SQL Editor 執行

---

## 資料匯入

- CSV 匯入 script：`scripts/import-from-csv.js`
- 匯入時間點：2026-05-05
- 匯入資料：11 筆 places